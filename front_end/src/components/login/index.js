import { connect } from 'react-redux'
import ComponentDisplay from './'

const mapStateToProps = state => ({})

const mapDispatchToProps = dispatch => ({})

export const Component = connect(mapStateToProps, mapDispatchToProps)(
  ComponentDisplay
)
