import { connect } from 'react-redux'
import TasksDisplay from './Tasks'

const mapStateToProps = state => ({})

const mapDispatchToProps = dispatch => ({})

export const Tasks = connect(mapStateToProps, mapDispatchToProps)(TasksDisplay)
