{
  "name": "task-board",
  "version": "0.0.0",
  "main": "app.js",
  "engines": {
    "node": "8.11.1"
  },
  "private": true,
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.16.4",
    "knex": "^0.15.2",
    "pg": "^7.6.1"
  }
}